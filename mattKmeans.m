function [labels, centroids] = mattKmeans(X,k,maxIter)
% mattKmeans performs k means clustering
% returns the cluster labels and centroids
% by Matt Churgin

iter=0;

% initialize cluster centers


myError =

% iterate to find cluster centers
while myError>0 || iter>maxiter
    
    
    iter=iter+1;
    display(['completed iteration ' num2str(iter)])
end